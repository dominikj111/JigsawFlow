# Rust Examples

Requires Rust 1.80+. Uses [`singleton-registry`](https://crates.io/crates/singleton-registry).

```sh
cargo run --example 01_capability_contract
cargo run --example 02_runtime_swap
cargo test
```
